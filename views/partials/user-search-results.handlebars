<div class="card-header m-2">
    <h2 class="blue-glowing-neon-text neon-tubes-lowercase m-0">Results for {{queryTerm}}</h2>
</div>
<div class="card-body">
    <div class="container-fluid">
        <div class="row">
            {{#each userResults}}
            {{> (whichUser ../this)}}
            {{/each}}
        </div>
    </div>
</div>