<div class="shadow-lg mb-3">
    <div class="card-header">
        <h5 class="purple-glowing-neon-text neon-tubes-lowercase m-0">{{title}}</h5>
    </div>
    <div class="card-body">
        <p class="card-text mb-1">{{{newsCleanUp contents}}}</p>
        <a href="{{url}}" target="_blank">Read more</a>
    </div>
</div>